/* 
 * This file lets you use TypeScript (https://typescriptlang.org), a 
 * superset of JavaScript that lets you catch code issues quickly.
 * Using TypeScript is entirely optional; to opt in, change a file's
 * extension to ".tsx" from ".js" or ".jsx".
 *
 * Even if you only use JavaScript, an editor like Visual Studio Code
 * (https://code.visualstudio.com) will still help you find issues.
 */
{
  "compilerOptions": {
    /* Feel free to change these TypeScript options or add more. */
    "strict": false,  /* Enable all strict type-checking options. */
    "checkJs": false, /* Perform type checking on JavaScript files. */

    /* These options are best left as-is. */
    "jsx": "react",
    "noEmit": true,
    "moduleResolution": "node",
    "esModuleInterop": true,                  
    "forceConsistentCasingInFileNames": true,
    "allowJs": true,
  },
  "include": ["src", "config", "modules"], /* All of the source files will be in one of these directories. */
  "exclude": ["**/.*"] /* Ignore hidden files beginning with "." */
}
